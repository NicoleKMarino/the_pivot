
<div id="items_list">
  <% @jobs.each do |job| %>
    <div class="well col-md-3 item-in-list job salary<%= job.salary[1] %> <%= job.company.location %>" data-status="<%= job.company.location %>" data-salary="<%= job.salary %>">
      <%= link_to "#{job.title}", job_path(job) %>
      <p>Company: <%= job.company.name %></p>
      <p>Location: <%= job.company.location %></p>
      <!-- <% link_to "Edit", edit_admin_item_path(item), class: "center_button btn btn-default" if current_admin? %> -->
      <div class="col-md-2 col-md-offset-4">
      <%=button_to "Apply",jobs_path, class:"btn btn-warning"%>
    </div>
      <%=button_to "Add to Favorite",jobs_path, class:"btn btn-primary"%>

    </div>
<% end %>
</div>
