<div class="job-show">
  <div class="well col-md-10">
    <h1><%= @job.title %></h1>
    <div class="job-content">
      <h2><p>Company: <strong><%= @job.company_name %></strong></h2></p>
      <p><%= @job.description %></p>
      <p><%= @job.salary %><p>
      <p><%= button_to 'Login or Create Account to Continue Application', login_path, method: :get, class: 'btn btn-default' %></p>
      <p><%= button_to 'Continue Browsing Jobs', jobs_path, method: :get, class: 'btn btn-default' %></p>
    </div>
  </div>
</div>
